# Local development overrides
# Note: For local development, do NOT set VITE_API_BASE_URL
# This allows the Vite proxy to handle API requests and avoid CORS issues
# VITE_API_BASE_URL=http://localhost:8000
